Upload the dataset on HF space.

# EVAL 
- fastAPI for json output of generated questions
- base model res
- trained model res
- integrate the monitoring platform something like comet, 

- The landscape of fine tuning techniques includes supervised fine-tuning, instruction fine tuning, full fine tuning, and parameter-efficient fine tuning (PEFT) methods.

## Best Practices and Common Pitfalls When Fine Tuning LLMs
- The best defenses are **data augmentation** (generating additional training examples that reflect the target distribution), PEFT methods that limit the number of trainable parameters, and early stopping based on validation loss.
